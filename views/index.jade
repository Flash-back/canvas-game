extends layout

block css
    link(rel='stylesheet', href='/stylesheets/estilos.css')

block js
    script(type='text/javascript').
      var logueado = 0;
      var usuarioDiv;
      var miUsuarioDiv;
      var miUsuarioDatos;
      var nombre;

        jQuery(function ($) {
        var setNombreForm = $('.login form');
        var users = $('.users');
        var usersList = $('.users ul');
        var messages = $('.messages');
        var messageForm = $('.message-box form');
        var message = $('.message');
        var wrapper = $('.wrapper');
        var login = $('.login');
        var gamescreen = $('#gamescreen');
        

        setNombreForm.submit(function(event) {
          $('.login').css( "display", "none" );
          event.preventDefault();
          console.log("Intento de login")
          socket.emit('login', $('#nombre').val(), function (data,callback) {
              nombre = $('#nombre').val();
              logueado = 1;
              console.log('Nombre de usuario configurado de modo correcto');
              wrapper.show();
          });
        });
        });
        socket.on('anuncio', function (data) {
          console.log("a");
          $('.messages').append('<p>' + data.message + '</p>');
        });
        socket.on('usuarios', function (data) {
          var usuariosConectados = '';
          var imagenUsuario = '';
          for (var i = 0; i < data.length; i++) {
            usuariosConectados += '<li><span>' + data[i]['nombre'] + '</span></li>';
            imagenUsuario = render(data[i]);
          }
          $('.users ul').empty().append(usuariosConectados);
        });

        socket.on('movimiento', function(data){
          if (data.eje == 'x'){
            margin = parseInt($('#usuario_'+data.id).css("margin-left"));
            $('#usuario_'+data.id).css('margin-left',margin + data.cantidad +"px");
          }
      
        });

        function render (usuario) {
        console.log(usuario);
          $('#gamescreen').append('<div class="usuario" id="usuario_'+usuario.id+'">'+usuario.nombre+'</div>');
         $('#usuario_'+usuario.id).css({'width':'15px','height':'15px','background':'red','margin-left':usuario.x});
          if (nombre == usuario.nombre) {
              miUsuarioDatos = usuario;
              miUsuarioDiv = $('#usuario_'+usuario.id);
          }
        };

        $(document).keypress(function(e) {
          if (logueado === 1){
            switch(e.keyCode){
              case 37:
                alert("a");
                break;
              case 38:
                alert("a");
                break;
              case 39:
               /* margin = parseInt(miUsuarioDiv.css("margin-left"));
                miUsuarioDiv.css('margin-left', margin+ 10+'px');
                miUsuarioDatos.x+=10;*/
                socket.emit('movimiento', {'eje':'x','cantidad':10});
                break;
              case 40:
                alert("a");
                break;
              default:
                console.log("Pulsaste la tecla con código: "+e.keyCode);
                break;
            }
          }
            
        }); 


block content
    section.login
      p Login
      form
        input(type='text', id='nombre')
        button Enviar

    section.wrapper
      section#gamescreen
      hr
      p Usuarios:<br/>
      section.messages
        section.users
          ul
